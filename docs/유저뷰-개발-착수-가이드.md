# 유저뷰 개발 착수 가이드

> 버전: v1.0 | 작성일: 2026.07.03 | 대상: 유저뷰 repo 개발 (공동작업자 + Claude Code)
> 전제 문서: `docs/00_데이터-계약-SSOT.md`(v1.1) · 유저뷰 상세기획 01~06 · 스핀오프 운영정책 · 큐레이터 페르소나 사양
> 원칙: **모든 필드명·enum·이벤트명은 SSOT를 그대로 사용한다. 임의 변형 금지.** (어드민 repo가 같은 이름으로 이미 바인딩됨)

---

## 1. 스택 · 구조 (어드민과 통일)

- **Vite + React + lucide-react** (어드민 repo와 동일 — 협업·코드 이동 용이)
- 모바일 웹앱 (v3.0 §9: 네이티브 앱 OUT). 기준 뷰포트 390px, 데스크톱은 센터 컬럼.
- 디자인 토큰(어드민 계승): 골드 `#C9A84C` / 다크 `#0D0D0F` / 배경 `#F7F5F0` / 서체 Playfair Display + DM Sans + DM Mono. 감정 axis 색상은 어드민 Monitor의 `EMOTION_AXES` 팔레트를 복사해 사용.

```
src/
  data/ssot.js          # SSOT 미러 (아래 §3) — 단일 소스
  lib/track.js          # 이벤트 발화 유틸 (아래 §4)
  lib/session.js        # 세션 상태 (visitor/session/exhibition 컨텍스트)
  lib/account.js         # 🆕 계정 상태 + 게스트→계정 병합 로직 (SSOT §7-2)
  components/           # 공통 컴포넌트 (아래 §5)
  nav/                   # 🆕 내비게이션 셸 (가이드/계정 2모드, IA §2·§6)
  screens/
    S1Onboarding.jsx    S2ArtworkCard.jsx   S3CuratorChat.jsx
    S4AfterglowReport.jsx S5SpinoffLanding.jsx S6PopupCurator.jsx
    S0Login.jsx          S7MyPage.jsx  S8MyReports.jsx  S9Invites.jsx  S10Home.jsx   # 🆕
```

## 2. 구현 순서 (마일스톤)

MVP는 목업 데이터 기반 정적 프로토타입 → 이후 API 바인딩 (어드민과 동일 전략).

| M | 범위 | 완료 기준 (DoD) |
|---|---|---|
| **M0 공통 기반** | ssot.js + track.js + session.js + 공통 컴포넌트 골격 | 이벤트가 콘솔/로컬 큐에 SSOT 규격으로 쌓임 |
| **M1 척추 A** | S1 온보딩 → S2 정보카드 | `session.start` 발화, 감정 칩이 `{emotion,axis}`로 저장, 30초 룰 통과 |
| **M2 척추 B** | S3 채팅 (mock 응답) | `curator.chat_start`, emotion_chips·depth_score 축적, 감정 칩 탭 동작 |
| **M3 전환부** | S4 리포트 → S5 랜딩 | `invite.issued`→`landing_viewed`→`redeemed`→`purchase.link_clicked` 전 퍼널이 mock으로 완주 가능 |
| **M4 팝업** | S6 팝업 큐레이터 | 존 리스트+취향 뱃지, `spinoff.*` 이벤트, S3 채팅 컴포넌트 재사용 확인 |
| **M4.5 계정 모드** 🆕 | S0 로그인·가입 → S7~S10 (마이페이지/마이리포트/초대장/홈) | `account.signup`/`login`/`visitor_merged` 발화, 게스트→계정 병합 데이터 정합, 열람 모드(S4/S5 크롬 전환) 동작, 홈 §A 우선순위 엔진 1건 노출 확인 |
| **M5 연결** | 실제 AI 응답(Claude API) + 어드민 mock 데이터 정합 확인 | 초대장 concept_type ↔ 어드민 ConceptBot enum 일치 |

- 순서 근거: 데이터가 흐르는 척추(1→2→3→4→5) 순. S6은 S3 재사용이라 마지막.
- 각 화면의 세부 사양·엣지 케이스는 해당 상세기획 문서의 §진입·이탈 / §상태·엣지 / §이벤트 매핑을 그대로 따른다.

## 3. `data/ssot.js` — SSOT 미러 (필수 상수)

```js
// SSOT v1.1 §2 — 어드민 Monitor와 동일 팔레트 유지
export const EMOTION_AXES = {
  serene:{ko:"고요·평온",color:"#7B9EE8"}, dreamy:{ko:"몽환·신비",color:"#9A7EE8"},
  melancholy:{ko:"우수·쓸쓸",color:"#6B8EAE"}, warm:{ko:"따뜻·포근",color:"#E8A84C"},
  awe:{ko:"경외·압도",color:"#4CAF7C"}, thrill:{ko:"강렬·고양",color:"#E87B7B"},
  tension:{ko:"긴장·불안",color:"#9A9490"}, contemplative:{ko:"사색·심오",color:"#C9A84C"},
};

// 전시별 EMOTION_LEAF (mock — '빛의 심연')
export const EMOTION_LEAFS = [
  { leaf:"고요함", axis:"serene" }, { leaf:"몽환적인", axis:"dreamy" },
  { leaf:"슬픔", axis:"melancholy" }, { leaf:"따뜻함", axis:"warm" },
  { leaf:"압도적", axis:"awe" }, { leaf:"해방감", axis:"thrill" },
  { leaf:"신비로움", axis:"dreamy" }, { leaf:"심오함", axis:"contemplative" },
];

export const CONCEPT_TYPES = { /* 어드민 ConceptBot.jsx의 CONCEPT_TYPES 복사 — 6종 + curator 직함 */ };

export const SEGMENTS = ["emotional_explorer","experience_seeker","companion_visitor","info_collector"];

// mock 전시·작품·존·초대장: 어드민 mock('빛의 심연'/'어비스 티 라운지', pj_abyss/pj_abyss_pop)과 id 통일
```

## 4. `lib/track.js` — 이벤트 규격 (SSOT §3·§3-1)

```js
// 퍼널: session.start | curator.chat_start | invite.issued |
//       invite.landing_viewed | invite.redeemed | purchase.link_clicked
// 팝업: spinoff.session_start | spinoff.zone_viewed | spinoff.curator_chat | landing.organic_viewed
export function track(event, extra = {}) {
  const p = { event, exhibition_id, session_id, ts: Date.now(), ...extra };
  queue.push(p);            // MVP: 로컬 큐 + console — 오프라인 시 재전송(화면2 §4)
}
// 1회성 이벤트(chat_start, landing_viewed, redeemed)는 track 내부에서 중복 발화 가드
```

## 5. 공통 컴포넌트

| 컴포넌트 | 사용 화면 | 핵심 규칙 |
|---|---|---|
| `EmotionChip` | S1·S2·S3·S6 | 표시만 `#`, 저장은 `{emotion,axis}`. active leaf만 노출 |
| `ChatThread` | S3·**S6 재사용** | 스트리밍 표시, 감정 칩 제안, 퀵 리플라이. S6은 role prompt만 주입 |
| `QRScanner` | S1·S2·S6 | 권한 거부 시 목록 경로 폴백(화면2 §4) |
| `PersonaCard` | S4·S5 | axis 상위 2종 그라디언트 + persona_copy. 이미지 저장(공유 단위) |
| `InviteCard` | S4·S5 | concept_type·personalized_copy·유효기간(팝업 종료일=운영정책 §3) |
| `BottomBar` | S2~S3 | "관람 완료" 전역 진입점 |

## 6. Claude API (S3·S6 대화)

- 아티팩트/프로토타입 단계: `claude-sonnet-4-6`, mock KB를 system prompt로 주입.
- 응답 규격: 본문 + `extracted_emotions:[{emotion,axis}]` + `depth_score:1~5` JSON — 화면3 §4-3.
- RAG 범위 제한(전시 KB 밖 금지)·세계관 내 회피는 system prompt에 명시. S6은 여기에 `curator_role_prompt`(페르소나 사양 §2) 추가.

## 7. repo 세팅 체크리스트

- [ ] `CLAUDE.md` 생성: "필드·enum·이벤트는 docs/00_데이터-계약-SSOT.md 준수 / 디자인 토큰은 본 가이드 §1 / 화면 사양은 docs/유저뷰-상세기획-0N 참조"
- [ ] 브랜치 전략: main 직push 금지, 화면 단위 feature 브랜치 → PR
- [ ] 어드민 repo mock id와 통일: `pj_abyss`(main) / `pj_abyss_pop`(spinoff) / concept id A·B·C
- [ ] 열린 결정 포인트(각 상세기획 말미)는 **현안대로 구현** — 변경 시 문서 먼저 수정 후 코드

## 8. 하지 않는 것 (MVP OUT — v3.0 §9 + 상세기획 종합)

네이티브 앱 / 자체 결제 / Vision-to-Text / 음성 입출력 / 다국어 / 푸시 알림 / 실시간 도면 / NFT 발급
